<!DOCTYPE html>
<html lang="id">
<head>
    <title>Akses Ditolak - Inventaris Baterai</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/navbar', { activePage: '' }) %>
    <main class="container-fluid px-4 pb-5">
        <section class="page-heading">
            <div>
                <p class="eyebrow mb-1 text-danger">403 — Akses Ditolak</p>
                <h1>Halaman Ini Tidak Dapat Diakses</h1>
                <p class="text-muted mb-0">
                    Role kamu (<strong><%= admin ? admin.role : 'unknown' %></strong>) tidak memiliki izin untuk mengakses halaman ini.
                    Hubungi owner jika ini adalah kesalahan.
                </p>
            </div>
        </section>
        <div class="content-panel" style="max-width:480px; margin-top:1rem;">
            <div class="empty-state py-5">
                <i class="bi bi-shield-lock" style="font-size:3rem; color:var(--danger-color, #dc3545);"></i>
                <span class="mt-3 text-muted">Tidak ada izin untuk halaman ini.</span>
                <a href="/" class="btn btn-primary mt-3">
                    <i class="bi bi-house"></i> Kembali ke Dashboard
                </a>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
